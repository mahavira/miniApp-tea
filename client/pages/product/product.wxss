.filter-wrp{
  position: fixed;
  left: 0;
  top: 0;
  width: 100%;
  box-sizing: border-box;
  padding:2px 15px;
  background: #FFF;
}
.filter {
  display: flex;
  height: 24px;
  color: #888;
  box-shadow: 0 0 2px #aaa;
  padding: 8px;
  font-size: 14px;
  background: #FFF;
}

.filter .input {
  font-size: 14px;
  flex: 1;
  height: 24px;
  line-height: 24px;
  box-sizing: border-box;
  color: #aaa;
}

.filter-icon {
  width: 155px;
  display: flex;
}

.btn {
  width: 60px;
  height: 25px;
  display: flex;
  justify-content: center;
  align-items: center;
  text-align: center;
  margin-left: 10px;
}

.btn image {
  width: 17px;
  height: 17px;
  margin-left: 5px;
}

.btn.btn-search image {
  width: 20px;
  height: 20px;
}

.btn.btn-search {
  width: 25px;
  position: relative;
}

.more {
  font-size: 14px;
  padding: 20px;
  color: #999;
  text-align: center;
}

.filter-panel {
  background: rgba(250, 250, 250, 0.8);
  position: fixed;
  left: 0;
  top: 0;
  width: 100%;
  height: 100%;
  z-index: 2;
  visibility: hidden;
  opacity: 0;
  transition: all 0.4s;
}

.filter-container {
  width: 80%;
  height: 100%;
  position: absolute;
  right: 0;
  top: 0;
  background: #fff;
  box-shadow: -1px 1px 5px rgba(0, 0, 0, 0.5);
  transform: translateX(100%);
  transition: all 0.4s;
  padding-bottom: 30px;
  box-sizing: border-box;
}
.mask{
  width: 100%;
  height: 100%;
  position: absolute;
  right: 0;
  top: 0;
}

.open.filter-panel {
  visibility: visible;
  opacity: 1;
}

.open .filter-container {
  transform: translateX(0%);
}

.term {
  margin: 10px 15px 20px;
}

.term-hd {
  font-size: 18px;
  color: #333;
  margin-bottom: 15px;
}

.term-opts {
  display: flex;
  flex-wrap: wrap;
  justify-content: space-between;
}

.term-opt {
  font-size: 14px;
  color: #555;
  background: #eee;
  flex: 0 0 45%;
  box-sizing: border-box;
  line-height: 1;
  padding: 10px;
  margin-bottom: 10px;
  transition: all 0.4s;
}

.term-opt.active {
  background: #999;
  color: #fff;
}

.term-btns {
  position: fixed;
  bottom: 0;
  right: 0;
  width: 80%;
  display: flex;
  border-top: 1px solid #eee;
}

.term-btn {
  flex: 1;
  background: RGBA(228, 27, 29, 1.00);
  color: #fff;
  font-size: 18px;
  line-height: 1;
  box-sizing: border-box;
  padding: 12px 20px;
  text-align: center;
}

.term-btn.close {
  background: #fff;
  color: #000;
}

.products{
  margin-top: 60px;
}